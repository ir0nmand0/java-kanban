package utils;
public class Main {

    public static void main(String[] args) {

/*
        //тесты
        task.TaskManager taskManager = new task.TaskManager();
        task.Task task1 = new task.Task("Задача 1", "Описание задачи 1", task.Status.NEW);
        task.Task task2 = new task.Task("Задача 2", "Описание задачи 2", task.Status.IN_PROGRESS);
        taskManager.addTasks(task1);
        taskManager.addTasks(task2);
        task.Epic epic1 = new task.Epic("Эпик1", "Описание Эпика 1");
        task.Epic epic2 = new task.Epic("Эпик2", "Описание Эпика 2");

        taskManager.addEpics(epic1);
        taskManager.addEpics(epic2);

        task.Subtask subtask1 = new task.Subtask("Подзадача 1", "Описание подзадачи 1", task.Status.NEW, epic1);
        task.Subtask subtask2 = new task.Subtask("Подзадача 2", "Описание подзадачи 2", task.Status.DONE, epic1);
        task.Subtask subtask3 = new task.Subtask("Подзадача 3", "Описание подзадачи 3", task.Status.DONE, epic2);

        taskManager.addSubtasks(epic1, subtask1);
        taskManager.addSubtasks(epic1, subtask2);
        taskManager.addSubtasks(epic2, subtask3);

        System.out.println("status epic1=" + epic1.getStatus());
        System.out.println("status epic2=" + epic2.getStatus());
        System.out.println("taskManager = " + taskManager);

        task.Subtask subtask4 = new task.Subtask("Подзадача 4", "Описание подзадачи 4", task.Status.NEW, epic1);
        task.Subtask subtask5 = new task.Subtask("Подзадача 5", "Описание подзадачи 5", task.Status.NEW, epic1);
        task.Subtask subtask6 = new task.Subtask("Подзадача 6", "Описание подзадачи 6", task.Status.DONE, epic2);

        taskManager.updateSubtasks(epic1, subtask1, subtask4);
        taskManager.updateSubtasks(epic1, subtask2, subtask5);
        taskManager.updateSubtasks(epic2, subtask3, subtask6);

        System.out.println("status epic1=" + epic1.getStatus());
        System.out.println("status epic2=" + epic2.getStatus());
        System.out.println("taskManager = " + taskManager);

        taskManager.removeTask(task1);
        taskManager.removeSubtask(epic2, subtask6);

        System.out.println("taskManager = " + taskManager);

        taskManager.clearTasks();
        taskManager.clearEpics();

        System.out.println("taskManager = " + taskManager);
*/

    }
}
